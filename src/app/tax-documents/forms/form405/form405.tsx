import styled from '@emotion/styled';

/* eslint-disable-next-line */
export interface Form405Props {}

const StyledForm405 = styled.div`
  color: pink;
`;

export function Form405(props: Form405Props) {
  return (
    <StyledForm405>
      <h1>Welcome to Form405!</h1>
    </StyledForm405>
  );
}

export default Form405;
