import styled from '@emotion/styled';

/* eslint-disable-next-line */
export interface Form30Props {}

const StyledForm30 = styled.div`
  color: pink;
`;

export function Form30(props: Form30Props) {
  return (
    <StyledForm30>
      <h1>Welcome to Form30!</h1>
    </StyledForm30>
  );
}

export default Form30;
