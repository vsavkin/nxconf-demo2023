import styled from '@emotion/styled';

/* eslint-disable-next-line */
export interface Form64Props {}

const StyledForm64 = styled.div`
  color: pink;
`;

export function Form64(props: Form64Props) {
  return (
    <StyledForm64>
      <h1>Welcome to Form64!</h1>
    </StyledForm64>
  );
}

export default Form64;
