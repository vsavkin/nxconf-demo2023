import styled from '@emotion/styled';

/* eslint-disable-next-line */
export interface Form148Props {}

const StyledForm148 = styled.div`
  color: pink;
`;

export function Form148(props: Form148Props) {
  return (
    <StyledForm148>
      <h1>Welcome to Form148!</h1>
    </StyledForm148>
  );
}

export default Form148;
